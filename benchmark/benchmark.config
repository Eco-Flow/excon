// Nextflow config applied to all benchmark runs.
// Sets overwrite = true so that -resume does not fail when trace/report/timeline
// files already exist from a previous interrupted run.

trace     { overwrite = true }
report    { overwrite = true }
timeline  { overwrite = true }
