params {
  input                      = "${projectDir}/data/input_small-s3.csv"  
  ensembl_biomart            = "metazoa_mart"
  ensembl_dataset            = "${projectDir}/data/biomart.txt"
  // Max resources for test input data can be reduced
  max_memory                 = '24.GB'
  max_cpus                   =  16
  max_time                   = '24.h'
}
