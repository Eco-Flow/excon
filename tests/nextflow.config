// Extra config applied on top of the chosen profile for every nf-test run.
// Nothing needed beyond the test_bacteria,docker profile today — kept as an
// explicit extension point, matching nf-core convention, rather than added
// only once something actually needs it.
params {
}
