name: Run Pipeline Tests

# Real end-to-end run of the pipeline on the smallest test dataset
# (test_bacteria: 4 tiny Mycoplasmoides genomes), on every PR targeting main.
# Not a stub run — most CAFE/GO modules don't have stub: blocks yet, so a
# -stub-run would feed real downstream processes with fake upstream output.
# Runs for real instead, small enough to still be fast.

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NXF_VER: 26.04.6

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4.1.1

      - name: Set up JDK 17
        uses: actions/setup-java@v4.0.0
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Nextflow
        run: |
          curl -s https://get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/
          nextflow -v

      - name: Run pipeline on test_bacteria
        run: |
          nextflow run main.nf \
            -profile test_bacteria,docker \
            --max_cpus 2 \
            --max_memory 6.GB
